#include<iostream>
#include<vector>
using namespace std;
int main(){
    // max profit by having of times in the range [idx, n-1] with remaining knapsack capacity as W
    // find the subsets of weights, there is non empty subset 
     vector<int> weights;
     vector<int> value;
    return 0;
}