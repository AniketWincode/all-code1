#include<iostream>
#include<vector>
using namespace std;
int main(){

    int n;
    cin>>n;
    vector<int> arr(n, 0);
    for(int i=0;i<n;i++){
        int y;
        cin>>y;
        arr.push_back(y);
    }
    vector<int> pse(n, 0);
    vector<int> nse(n, 0);
    for(int i=0;i<n;i++){
        int left =i
        int right = i;
        while(left>=0){
            if()
            left--;
        }
        while(right<n){
            right++;
        }
    }
    return 0;
}